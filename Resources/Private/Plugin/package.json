{
  "private": true,
  "scripts": {
    "build": "rm -rf ../../Public/Plugin && node build.js",
    "watch": "rm -rf ../../Public/Plugin && node build.js --watch"
  },
  "main": "./src/index.js",
  "devDependencies": {
    "@neos-project/neos-ui-extensibility": "^7.3",
    "esbuild": "^0.14.31"
  },
  "neos": {
    "buildTargetDirectory": "../../Public/Plugin"
  },
  "dependencies": {
    "emmet-monaco-es": "^5.1.0"
  }
}
