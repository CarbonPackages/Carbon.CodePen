# Autogenerated by "tailwind.config.js"

# clientTailwindConfig: ${File.read(FLOW_PATH_ROOT + 'tailwind.config.js')}
# cant work, since it's read by php, there might be requires' (like the `theme` function) and so it can't be eval'd on client

# best to generate this file via js

# (function dumpClientTailwindConfigToYaml(tailwindConfig) {
#     const clientTailwind = {
#         theme: tailwindConfig.theme
#     }
#     const yaml = `# Autogenerated by "tailwind.config.js"
# Carbon:
#   CodeEditor:
#     clientTailwindConfig: |
#       ${JSON.stringify(clientTailwind, null, '  ').replaceAll("\n", "\n      ")}
# `
#     fs.writeFileSync('./Configuration/Settings.ClientTailwindConfig.yaml', yaml)
# })(module.exports)

Carbon:
  CodeEditor:
    clientTailwindConfig: |
      {
        "theme": {
          "spacing": {
            "sm": "8px",
            "md": "12px",
            "lg": "16px",
            "xl": "80px"
          },
          "extend": {
            "colors": {
              "marcHenrysFarbe": {
                "50": "#EBF1FC",
                "100": "#D3E0F8",
                "200": "#A3BEF0",
                "300": "#739CE7",
                "400": "#437ADF",
                "500": "#225CC9",
                "600": "#1B4AA2",
                "700": "#15387A",
                "800": "#0E2653",
                "900": "#07142C",
                "DEFAULT": "#225CC9"
              }
            }
          }
        }
      }
