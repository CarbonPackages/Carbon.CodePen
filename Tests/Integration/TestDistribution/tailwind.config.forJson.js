// rename file to `tailwind.config.js` before running "pnpm run jsonTailwindConfig"

module.exports = {
    theme: {
        extend: {
            colors: {
                'marcherryJson': '#D2042D',
            },
            spacing: {
              'makeTheButtonBiggerJson': '42rem',
            },
        },
    },
}
